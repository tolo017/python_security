# A single variable (Day 1)
target_system = "Server_A"

# A list of multiple systems (Day 2)
network_systems = ["Server_A", "Server_B", "Firewall", "Database"]

# Computers start counting from 0, so the first item is at index 0
print(network_systems[0])
# This will output: Server_A
